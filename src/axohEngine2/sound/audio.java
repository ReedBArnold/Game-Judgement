package axohEngine2.sound;
import java.io.*;
import javax.sound.sampled.AudioInputStream;
import javax.sound.sampled.AudioSystem;
import javax.sound.sampled.Clip;
import sun.applet.*;
import sun.audio.*;
/** * A simple Java sound file example (i.e., Java code to play a sound file).
 *  * AudioStream and AudioPlayer code comes from a javaworld.com example. */
/*
public class audio { public static void play() { 
	try {// open the sound file as a Java input stream    
	String gongFile = "audio/audio2.wav";    
	InputStream in = new 
	FileInputStream(gongFile);// create an audiostream from the inputstream    
	AudioStream audioStream = new AudioStream(in);// play the audio clip with the audioplayer class    
	AudioPlayer.player.start(audioStream); 
	} catch (IOException e) 
	{    e.printStackTrace();    } }}

*/

